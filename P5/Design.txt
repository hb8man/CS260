The general idea behind this program is similar to the last assignment, but in this case we need to add
some additional logic into the add function. We need to handle if the list is empty, and if it isn't
loop through the list to find where the new one belongs when adding.